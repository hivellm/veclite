## 1. Implementation
- [x] 1.1 Incremental add_document (EMB-030) on the Embedder trait (default no-op for stateless providers) + implementations for bm25 (df/doc-count/avgdl updates, deterministic new-term append while space remains), tfidf (new serde-default df/total tables — pre-3f state imports fine and upgrades at the next refit — full IDF refresh per add), bow and char_ngram (term/n-gram append). upsert_text is O(doc); indices never move so stored vectors stay comparable; refit stays the exact recompute (EMB-031).
- [x] 1.2 VOCAB persistence (EMB-030/032): export_state sealed as a VOCAB segment at checkpoint; imported on open (text_dirty=false — reopen searches identically with NO rebuild and NO re-embedding, proven by byte-identical stored vectors + zero tombstones); legacy files (text docs, no VOCAB) refit on first search. Exact crash replay: checkpoint state + add_document folded per replayed WAL text doc (checkpoint-loaded points install WITHOUT folding — their contribution is already in the sealed state), and refit journals a full VOCAB_UPDATE snapshot (WAL op 7) AFTER its re-upsert batches so replay converges to the exact fitted state.
- [x] 1.3 Database::register_embedder(name, Box<dyn Embedder>) per instance (EMB-011): built-in/duplicate names → AlreadyExists; one shared instance (Arc<Mutex>) per name across collections. Reopen resolves providers LENIENTLY: built-in → registered → a Missing slot — open succeeds, vector ops work, text ops fail with UnsupportedProvider naming the remedy; registering binds deferred collections and imports any VOCAB state carried through open (pending_vocab survives checkpoints while the provider is absent).
- [x] 1.4 svd provider behind the `svd` feature: vendored from the server (hash-seeded orthonormal projection over a TF-IDF base, Gram–Schmidt + normalize, identical math); ndarray Array2 replaced by a plain Vec matrix (zero new deps); unfitted embed returns zeros to follow the collection lifecycle (documented adaptation).
- [x] 1.5 onnx/fastembed (EMB-040/041): the EMB-023 mechanism ships NOW — `fastembed:*` recognized (`is_onnx_provider`), collections referencing it open fine on non-onnx builds, vector reads/searches work, the first text op fails with UnsupportedProvider naming the `onnx` feature. The fastembed dependency + separate distribution artifacts are DAG T5.4's deliverable (phase5c_onnx-feature, already a scheduled task) — not deferred here, sequenced there by the DAG.

## 2. Testing
- [x] 2.1 Server parity corpus (acceptance 1): fixtures generated by compiling the UNMODIFIED Vectorizer provider sources (vectorizer/src/embedding/providers/*) over a fixed 8-doc corpus; tests/embedding_parity.rs asserts bm25/tfidf/bow/char_ngram reproduce every component of 11 inputs × 64 dims within 1e-5.
- [x] 2.2 tests/embedding_lifecycle.rs: reopen serves identical results+scores with byte-identical vectors and zero tombstones (no re-embed); crash replay (upserts + refit snapshot + more upserts) reproduces exact scores; incremental-then-refit equals scratch-fit.
- [x] 2.3 register_embedder: works for text ops; built-in/duplicate rejection; reopen-without-registration defers (vector ops OK, text ops UnsupportedProvider with "register_embedder" in the message); late registration re-binds; unknown provider at create still fails fast listing built-ins + registered names.

## 3. Tail (docs + tests — check or waive with tailWaiver)
- [x] 3.1 Update or create documentation covering the implementation — SPEC-005 status (EMB-011/023/030/031/032 + svd + parity delivered; fastembed → phase5c per DAG); CHANGELOG + README; rustdoc on the trait method, slots, and register_embedder.
- [x] 3.2 Write tests covering the new behavior — 5 lifecycle tests + 1 parity test + 3 svd unit tests; plus the SMALL_EXACT_MAX determinism guard (small live sets always brute-force — kills the tiny-graph flake class).
- [x] 3.3 Run tests and confirm they pass — 302 workspace tests green; clippy -D warnings clean (all features incl. svd); wasm32 build green.
