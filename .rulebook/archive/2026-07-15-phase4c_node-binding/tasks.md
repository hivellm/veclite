## 1. Implementation
- [x] 1.1 Context: read SPEC-010 in full + SPEC-004 mirrored surface (DAG T4.4). Modeled the crate on veclite-py (workspace-excluded, cdylib, binds veclite directly).
- [x] 1.2 crates/veclite-node skeleton: napi-rs 2.x (napi4/tokio_rt/serde-json), build.rs (napi_build), package.json (napi config, engines >=18), version fn (formatVersion). Added to the workspace `exclude`.
- [x] 1.3 Database/Collection classes with #[napi(object)] options mirroring OpenOptions/CollectionOptions/query/scroll/hybrid; absent fields take SPEC-004 defaults (NODE-010).
- [x] 1.4 Async-by-default via tokio spawn_blocking off the JS thread (open/createCollection/upsert/upsertBatch/upsertText/search/searchText/checkpoint/snapshot/vacuum/close) + `*Sync` twins (NODE-011).
- [x] 1.5 Float32Array in (search/upsert/upsertBatch) — zero-copy on the sync path (async copies once, off-thread requirement) — and hit vectors out as Float32Array (NODE-012). External-buffer finalizer optimization → phase4i.
- [x] 1.6 Handle lifecycle (NODE-013): Database holds Option<VecLite>; close() checkpoints then drops the handle (releases the advisory lock immediately), idempotent; ops after close → Closed. emitWarning-on-leaked-handle → phase4i.
- [x] 1.7 VecLiteError with code strings mirroring the FFI constants sans prefix; message identical to Rust. Native encodes `CODE\u{1}message`; the hand-written veclite.js loader splits it into one `VecLiteError extends Error` + `.code` (NODE-020).
- [x] 1.8 .d.ts generated by napi + veclite.d.ts re-export with VecLiteError; the quickstart compiles under `tsc --strict` (NODE-003, acceptance 5).

## 2. Testing
- [x] 2.1 Event-loop liveness (`__test__/veclite.test.mjs`): a concurrent 5 ms timer keeps ticking through a 4 000-point async bulk index; max gap asserted < 250 ms (NODE-011, acceptance 3).
- [x] 2.2 Float32Array in/out verified (upsertBatch + search withVector returns a Float32Array with the exact bytes). Allocation-tracking zero-copy-out proof → phase4i (needs the external-buffer path).
- [x] 2.3 Error-code mapping: DIMENSION_MISMATCH / COLLECTION_NOT_FOUND / UNSUPPORTED_PROVIDER; async and sync twins yield the same code + message; CLOSED after close.
- [x] 2.4 Quickstart e2e: memory + cross-process file persistence (child-process writer so the lock releases on exit), snapshot, vacuum, scroll, filters, hybrid, auto-embed text. The Node 18/20/22 + Bun prebuild matrix run → phase4i (needs CI, currently disabled).

## 3. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 3.1 Update or create documentation covering the implementation — crates/veclite-node/README.md; SPEC-010 status; CHANGELOG; rustdoc + generated .d.ts.
- [x] 3.2 Write tests covering the new behavior — 7 behavioral tests (node --test) + the tsc --strict quickstart check.
- [x] 3.3 Run tests and confirm they pass — 7/7 node tests green; cargo clippy + fmt clean on the crate; tsc --strict exit 0; the pure-Rust workspace gate (308 tests, clippy, wasm) unaffected by the new exclude.
