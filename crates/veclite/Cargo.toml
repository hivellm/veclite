[package]
name = "veclite"
description = "Embedded, single-file, in-process vector database."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["vector-database", "embeddings", "search", "embedded", "hnsw"]
categories = ["database", "data-structures"]

[features]
default = ["simd"]
# Enables the vendored SIMD distance kernels once they land in phase1b
# (ADR-0001: vendored from hivellm/vectorizer, no external dependency).
simd = []

[dependencies]
thiserror.workspace = true

[lints]
workspace = true
